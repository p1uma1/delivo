apiVersion: apps/v1
kind: Deployment
metadata:
  name: cart-service
  namespace: delivo
  labels:
    app: cart-service
spec:
  replicas: 1
  selector:
    matchLabels:
      app: cart-service
  strategy:
    type: RollingUpdate
    rollingUpdate:
      maxUnavailable: 0
      maxSurge: 1
  template:
    metadata:
      labels:
        app: cart-service
    spec:
      containers:
        - name: cart-service
          image: delivoregistry123.azurecr.io/cart-service:latest
          ports:
            - containerPort: 3005
          envFrom:
            - configMapRef:
                name: delivo-config
            - secretRef:
                name: delivo-secrets
          env:
            - name: PORT
              value: "3005"
          resources:
            requests:
              cpu: "50m"
              memory: "64Mi"
            limits:
              cpu: "200m"
              memory: "256Mi"
          readinessProbe:
            tcpSocket:
              port: 3005
            initialDelaySeconds: 10
            periodSeconds: 5
            failureThreshold: 3
          livenessProbe:
            tcpSocket:
              port: 3005
            initialDelaySeconds: 20
            periodSeconds: 15
            failureThreshold: 3
---
apiVersion: v1
kind: Service
metadata:
  name: cart-service
  namespace: delivo
spec:
  selector:
    app: cart-service
  ports:
    - port: 3005
      targetPort: 3005
